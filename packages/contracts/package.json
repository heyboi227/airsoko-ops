{
  "name": "@airsoko/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared wire format: enums, schemas and error codes used by both the API and the web client.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit --project ."
  },
  "dependencies": {
    "zod": "^4.5.4"
  }
}
