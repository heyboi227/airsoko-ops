# Engines are a promise, not a suggestion. Without this npm downgrades a
# version mismatch to a warning nobody reads -- which is how a Node 22 / npm 10
# shell rewrote a lockfile that npm 11 had written, silently dropping the
# `libc` fields that select musl vs glibc binaries.
engine-strict=true
